name: Heroku Awake
on:
  schedule:
    - cron: "*/1 * * * *"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: satak/webrequest-action@master
        with:
          url: 'https://little-pig-bot.herokuapp.com'
          method: GET
#      - uses: mikeesto/heroku-awake@1.0.0
#        with:
#          URL: 'https://little-pig-bot.herokuapp.com'

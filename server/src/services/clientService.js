const logger = require('../helper/logger');

module.exports = class ClientService {
  static async sendToClients(data, clients) {
    logger.debug('TODO implement');
    logger.debug(data);
    logger.debug(clients);
  }
};
